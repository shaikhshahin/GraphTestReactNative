/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Speedometer from './src/components/SpeedometerGraph';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Speedometer);
